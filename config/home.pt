<!DOCTYPE html>
<html lang="en">
<head>
<!-- This HTML template will be used by the Chameleon templater to pass the name variables set in views.py-->
    <title>${name}</title>
</head>
<body>
<h1>${name}</h1>
<a href="${request.route_url('home')}">${home}</a>
<br>
<a href="${request.route_url('docs')}">${docs}</a>
<br>
<a href="${request.route_url('jupyter')}">${jupyter}</a>
<br>
<a href="${request.route_url('weaver')}">${weaver}</a>
<br>
<a href="${request.route_url('catalog')}">${catalog}</a>
<br>
<a href="${request.route_url('login')}">${login}</a>
<br>
<a href="${request.route_url('admin')}">${admin}</a>


</body>
</html>
